spring:
  jpa:
    database: mysql
    open-in-view: false
    show-sql: false
    hibernate.ddl-auto: validate
    properties:
      hibernate:
        format_sql: false
        highlight_sql: false
---
spring:
  config.activate.on-profile: local
  jpa:
    properties:
      hibernate:
        format_sql: true
        highlight_sql: ture
